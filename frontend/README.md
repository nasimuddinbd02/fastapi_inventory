# FastAPI Inventory Frontend

This is a minimal Next.js frontend scaffold for the FastAPI inventory backend.

Quick start:

1. cd frontend
2. npm install
3. npm run dev

Default API base used in the scaffold: `http://localhost:8000` — adjust code if your backend runs elsewhere.
